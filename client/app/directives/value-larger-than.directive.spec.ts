import { ValueLargerThanDirective } from './value-larger-than.directive';

describe('ValueLargerThanDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueLargerThanDirective();
    expect(directive).toBeTruthy();
  });
});
