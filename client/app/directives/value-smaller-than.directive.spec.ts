import { ValueSmallerThanDirective } from './value-smaller-than.directive';

describe('ValueSmallerThanDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueSmallerThanDirective();
    expect(directive).toBeTruthy();
  });
});
